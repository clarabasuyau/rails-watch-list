<h2>Add a movie</h2>

<%= simple_form_for([@list, @bookmark])do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.button :submit %>
<% end %>

<%= link_to "back to lists", root_path %>
